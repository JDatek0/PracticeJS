
--------Responde las siguientes preguntas en la sección de comentarios-------------

¿Qué es una variable y para qué sirve?
	R= es un espacio en memoria que permite guardar datos. estos nos pueden servir para luego operar con ellos.

¿Cuál es la diferencia entre declarar e inicializar una variable?
	R= al momento de declarar una variable no se escribe el valor que va a tomar, unicamnete se crea el espacio en memoria con el nombre que le hayamos dado, en cambio al momento de inicializar se especifica el valor de la variable.

¿Cuál es la diferencia entre sumar números y concatenar strings?
	R= La diferencia en si es el tipo de dato, ya que para concatenar datos se utiliza el tipo de dato string que son cadenas de texto, mientras que para sumar 2 datos se utilzia el tipo number, un ejemplo de esto sería:
2 + 2 = 4 // Acá esta sumando
"2" + "2" = "22" Acá esta concatenando los datos.

¿Cuál operador me permite sumar o concatenar?
	R= el operador de suma osea el '+'.


----------Determina el nombre y tipo de dato para almacenar en variables la siguiente información-----------


    Nombre = String
    Apellido = String
    Nombre de usuario en Platzi = String
    Edad = number
    Correo electrónico = string
    Mayor de edad = booleano
    Dinero ahorrado = number
    Deudas = number

----------- Traduce a código JavaScript las variables del ejemplo anterior y deja tu código en los comentarios---------------------
	R= esta en el archivo pruebasJs en esta carpeta


-----------Calcula e imprime las siguientes variables a partir de las variables del ejemplo anterior------

    Nombre completo (nombre y apellido)
    Dinero real (dinero ahorrado menos deudas)
	R= Esta en el archivo prueba.js en esta misma carpeta.


-----------Responde las siguientes preguntas en la sección de comentarios:------------------

¿Qué es una función?
    R= una funcion es un espacio de codigo donde podemos dejar implementada una serie de acciones o algoritmo, que nos permitirapoder hacer uso de ella en cualquier momento solo invocandola y pasandole los parametros que requiera para funcionar

¿Cuándo me sirve usar una función en mi código?
    R= es recomendable usar una función cuando se pueda reutilizar ese pedazo de codigo en varias secciones, un ejemplo seria realizar una funcion que permita recuperar una serie de datos, y ordenarlos, para no volver a escribir el algoritmo que lo hace para reutilizarlo en otro lado, es tan facil como invocar otra vez a la funcion.

¿Cuál es la diferencia entre parámetros y argumentos de una función?
    R= los parametros de una funcion es los datos que necesita para poder funcionar correctamente. mientras que los argumentos son las variables locales de las mismas funciones.

--------Convierte el siguiente código en una función, pero, cambiando cuando sea necesario las variables constantes por parámetros y argumentos en una función--------
    R= Esta en el archivo prueba.js



---------------------------------------Responde las siguientes preguntas en la sección de comentarios:---------------------------------------

¿Qué es un condicional?
    R= es un pedazo de codigo que permite validar si una sentencia es correcta, si es asi ejecutar unas determinadas lineas de codigo, en dado caso que no sea verdad ejecutar otras lineas o en su defecto no hacer nada

¿Qué tipos de condicionales existen en JavaScript y cuáles son sus diferencias?
    R= existen 2 el if y el switch, mientras que el if se puede traducir como si pasa esto entonces que ocurra esto, y si es falso que ocurra esto otro, el switch te permite validar casos especificos, ejemplo si el numero es 1 que haga eso, si es 2 que haga esto otro y asi sucesivamente el numero de casos que se necesite.

¿Puedo combinar funciones y condicionales?
    R= Claro que se puede, asi permite reutilziar las condicionales y no tener que escribirlas nuevamente.

Replica el comportamiento del siguiente código que usa la sentencia switch utilizando if, else y else if:
    R= la respuesta esta en el archivo pureba.js de esta misma carpeta

Replica el comportamiento de tu condicional anterior con if, else y else if, pero ahora solo con if (sin else ni else if)
    R=la respuesta esta en el archivo pureba.js de esta misma carpeta

---------------------------------------Responde las siguientes preguntas en la sección de comentarios:---------------------------------------
¿Qué es un ciclo?
    R= es un pedazo de codigo que permite repetir las mismas lineas de codigo una determinada o aveces no determinada cantidad de veces

¿Qué tipos de ciclos existen en JavaScript?
    R= tenemos el ciclo for y el while


¿Qué es un ciclo infinito y por qué es un problema?
    R=Un ciclo infinito es aquel que no termina, y es un problema porque al no acabar se seguiran realizando operaciones hasta el infinito, ye sto satura el procesador y puede dejar inutilizable la maquina en el peor d elos casos

¿Puedo mezclar ciclos y condicionales?
    R= claro que si se puede, utilizando la sintaxis correcta, es posible que un ciclo este comprobando continuamente si una condicion se cumple.

 Replica el comportamiento de los siguientes ciclos for utilizando ciclos while
    R=la repuesta esta en el archivo prueba.js

Escribe un código en JavaScript que le pregunte a los usuarios cuánto es 2 + 2. Si responden bien, mostramos un mensaje de felicitaciones, pero si responden mal, volvemos a empezar.Pista: puedes usar la función prompt de JavaScript.
    R= La respuesta esta en el archivo prueba.js

---------------------------------------Responde las siguientes preguntas en la sección de comentarios:---------------------------------------

¿Qué es un array?
    R= es un espacio en memoria que puede guardar multiples valores, dependiendo del lugar que ocupe en el array

¿Qué es un objeto?
    R= es un espacio en memoria que guarda información, y esta puede contener, tanto parametros, como metodos que puede realizar el objeto

¿Cuándo es mejor usar objetos o arrays?
    R= cuando necesites que algo realize una actividad pues se utilizan objetos, sino es mejor solo usar un array, aunq de porsi en js un array es una variable tipo objetos, tambien se puede emplear un objeto cuando necesitamos representar algo de la vida real

¿Puedo mezclar arrays con objetos o incluso objetos con arrays?
    R= si, un objeto puede tener como parametro un array dentro suyo, o en un array se pueden guardar multiples objetos

Crea una función que pueda recibir cualquier array como parámetro e imprima su primer elemento.
    R=la respuesta esta en el archivo pueba.js

Crea una función que pueda recibir cualquier array como parámetro e imprima todos sus elementos uno por uno (no se vale imprimir el array completo).
    R= La respuesta esta en el archivo prueba.js

Crea una función que pueda recibir cualquier objeto como parámetro e imprima todos sus elementos uno por uno (no se vale imprimir el objeto completo).
    R= La respuseta esta en el archivo prueba.js


    
